{% extends 'base-back.html.twig' %}

{% block title %}Tournois{% endblock %}

{% block body %}
    <h1>Tournois</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tournoi.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ tournoi.nom }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ tournoi.dateDebut ? tournoi.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ tournoi.dateFin ? tournoi.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Prime</th>
                <td>{{ tournoi.prime }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tournois_index') }}">back to list</a>

    <a href="{{ path('tournois_edit', {'id': tournoi.id}) }}">edit</a>

    {{ include('tournois/_delete_form.html.twig') }}
{% endblock %}
