{%extends 'base-back.html.twig' %}
{% block title %}
    update prod
{% endblock %}
{%block css  %}{% endblock %}
{%block home%} {% endblock%}
{% block landing %} {% endblock %}
{% block tournament%}{% endblock%}
{% block pro%}{% endblock%}
{% block blog%}active{% endblock %}
{% block store%}{% endblock %}
{% block script %}{% endblock %}
{% block  body%}

    <div class="container">
        <h1>UPDATE</h1>
        {{ form_start(f) }}
        <table>
            <tr>
                <td>

                    {{ form_label(f.email,"Email : ") }}
                </td><td>
                    {{ form_widget(f.email) }}
                </td>
                <td>{{ form_errors(f.email) }}</td>
            </tr>
            <tr>
                <td>

                    {{ form_label(f.name,"Name : ") }}
                </td><td>
                    {{ form_widget(f.name) }}
                </td>
                <td>{{ form_errors(f.name) }}</td>
            </tr>
            <tr>
                <td>

                    {{ form_label(f.lastname,"LastName : ") }}
                </td><td>
                    {{ form_widget(f.lastname) }}
                </td>
                <td>{{ form_errors(f.lastname) }}</td>
            </tr>

            <tr>
                <td>

                    {{ form_label(f.imageFile,"Image : ") }}
                </td><td>
                    {{ form_widget(f.imageFile) }}
                </td>
                <td>{{ form_errors(f.imageFile) }}</td>
            </tr>
            <tr>
                <td>

                    {{ form_label(f.roles,"roles : ") }}
                </td><td>
                    {{ form_widget(f.roles) }}
                </td>
                <td>{{ form_errors(f.roles) }}</td>
            </tr>

        </table>
        {{ form_end(f) }}
        <div class="nk-gap-2 d-none d-lg-block"></div>
        <div class="nk-gap-2 d-none d-lg-block"></div>
        <div class="nk-gap-2 d-none d-lg-block"></div>
        <div class="nk-gap-2 d-none d-lg-block"></div>
        <div class="nk-gap-2 d-none d-lg-block"></div>
    </div>

{% endblock %}
