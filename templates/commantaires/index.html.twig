{% extends 'base-back.html.twig' %}

{% block title %}Commantaires index{% endblock %}

{% block body %}
    <h1>Commantaires index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Contenu</th>
                <th>DateCommentaire</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commantaire in commantaires %}
            <tr>
                <td>{{ commantaire.id }}</td>
                <td>{{ commantaire.contenu }}</td>
                <td>{{ commantaire.dateCommentaire ? commantaire.dateCommentaire|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    {{ include('commantaires/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('commantaires_new') }}">Create new</a>
{% endblock %}
